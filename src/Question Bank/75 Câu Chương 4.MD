Chương 4. 	Ngăn xếp, hàng đợi
Câu 1. [R]	Trong một ngăn xếp rỗng, để thêm 4 phần tử và sau đó loại bỏ tất cả, tổng số thao tác push và pop là ____.
[<A>]	8
Câu 2.
[R]	Sau khi push lần lượt 3 phần tử: 10, 20, 30 vào ngăn xếp, phần tử nằm trên đỉnh (top) là ____.
[<A>]	30
Câu 3.
[R]	Một hàng đợi chứa tối đa 6 phần tử. Nếu đang có 4 phần tử, số phần tử có thể thêm vào nữa là ____.
[<A>]	2
Câu 4.
[R]	Trong hàng đợi tuyến tính có 4 phần tử, phần tử nào được loại bỏ đầu tiên?
[<A>]	1
Câu 5.
[R]	Nếu thực hiện 5 lần push và 2 lần pop trên một ngăn xếp rỗng, số phần tử còn lại trong ngăn xếp là ____.
[<A>]	3
Câu 6.
[R]	Một mảng hàng đợi (queue) tuyến tính có kích thước 7. Số phần tử tối đa có thể chứa là ____.
[<A>]	7
Câu 7.
[R]	Trong chương trình tính giá trị biểu thức hậu tố (postfix), với 5 toán hạng và 4 toán tử, số lần gọi push là ____.
[<A>]	5
Câu 8.
[R]	Trong hàng đợi có 8 phần tử, nếu thực hiện 3 thao tác dequeue và 2 thao tác enqueue, thì số phần tử còn lại là ____.
[<A>]	7
Câu 9.
[R]	Một hàng đợi ban đầu rỗng, sau khi thêm 3 phần tử theo thứ tự A, B, C, phần tử được lấy ra đầu tiên sau thao tác dequeue là phần tử thứ mấy đã được thêm vào?
[<A>]	1
Câu 10.
[R]	Một ngăn xếp đang có 2 phần tử. Sau khi thực hiện 2 thao tác pop, ngăn xếp còn lại ____ phần tử.
[<A>]	0
Câu 11.
[U]	Một ngăn xếp rỗng được thực hiện các thao tác theo thứ tự:
push(1), push(2), pop(), push(3), pop().
Sau các thao tác trên, phần tử còn lại trong ngăn xếp là ____.
[<A>]	1
Câu 12.
[U]	Một hàng đợi rỗng nhận 4 thao tác enqueue và 2 thao tác dequeue. Số phần tử còn lại trong hàng đợi là ____.
[<A>]	2
Câu 13.
[U]	Cho hàng đợi ban đầu rỗng. Thực hiện 
enqueue(5), enqueue(7), dequeue(), enqueue(9).
Sau các thao tác, phần tử ở đầu hàng đợi là ____.
[<A>]	7
Câu 14.
[U]	Trong quá trình tính giá trị biểu thức hậu tố 5 6 2 + * 3 -, độ sâu tối đa của ngăn xếp là ____.
[<A>]	3
Câu 15.
[U]	Thực hiện thao tác 
enqueue(1), enqueue(2), enqueue(3), dequeue(), enqueue(4), dequeue().
Phần tử ở đầu hàng đợi sau cùng là ____.
[<A>]	3
Câu 16.
[U]	Ngăn xếp có 6 phần tử. Cần bao nhiêu thao tác pop() để làm rỗng?
[<A>]	6
Câu 17.
[U]	Thực hiện trên một ngăn xếp rỗng:
push(1), push(2), push(3), pop(), push(4), push(5), pop().
Số phần tử còn lại là ____.
[<A>]	3
Câu 18.
[U]	Thực hiện push(10), push(20), pop(), pop() trên một ngăn xếp rỗng ban đầu. Sau thao tác này, số phần tử trong ngăn xếp là ____.
[<A>]	0
Câu 19.
[U]	Thực hiện lần lượt: 
enqueue(1), enqueue(2), enqueue(3), dequeue(), enqueue(4)
→ Phần tử cuối hàng đợi (rear) là ____.
[<A>]	4
Câu 20.
[U]	Một hàng đợi ban đầu rỗng thực hiện các thao tác sau:
enqueue(10), enqueue(20), dequeue(), enqueue(30), enqueue(40), dequeue()
Số phần tử còn lại trong hàng đợi là ____.
[<A>]	2
Câu 21.
[AP]	Một ngăn xếp rỗng thực hiện lần lượt các thao tác: push(5), push(8), pop(), push(3), pop() sẽ trả về giá trị cuối cùng là: ___
[<A>]	3
Câu 22.
[AP]	Cho một hàng đợi rỗng, thực hiện các thao tác: enqueue(4), enqueue(7), dequeue(), enqueue(9) và dequeue(). Giá trị được trả về ở lần dequeue() cuối là: ___
[<A>]	7
Câu 23.
[AP]	Nếu một hàng đợi có kích thước tối đa là 5, sau 6 lần enqueue liên tiếp (không có dequeue), số lần thao tác enqueue sẽ bị từ chối là: ___
[<A>]	1
Câu 24.
[AP]	Cho biểu thức hậu tố: 6 2 3 + -, giá trị kết quả tính được khi thực hiện bằng ngăn xếp là: ___
[<A>]	1
Câu 25.
[AP]	Một hàng đợi tròn (circular queue) có front = 2, rear = 4, kích thước tối đa 6. Sau khi thực hiện 3 phép dequeue(), chỉ số mới của front là: ___
[<A>]	5
Câu 26.
[AP]	Trong một thuật toán kiểm tra dấu ngoặc hợp lệ (sử dụng stack), chuỗi ({[]}) sẽ thực hiện tổng cộng ___ thao tác push lên stack.
[<A>]	3
Câu 27.
[AP]	Cho hàm đệ quy gọi chính nó 3 lần trong mỗi lời gọi và độ sâu tối đa là 2, tổng số lời gọi hàm (kể cả lời gọi đầu tiên) là: ___
[<A>]	13
Câu 28.
[AP]	Số lượng phép so sánh tối đa để đảo ngược một ngăn xếp có 4 phần tử bằng cách sử dụng một ngăn xếp phụ và đệ quy là: ___
[<A>]	6
Câu 29.
[AP]	Thời gian thực hiện trung bình của một phép pop() trong ngăn xếp có độ dài n là ___ nếu cài đặt bằng mảng. 
[<A>]	1
Câu 30.
[AP]	Giả sử hàng đợi ưu tiên được cài bằng cây nhị phân tìm kiếm cân bằng (AVL), thời gian trung bình để thêm một phần tử là log₂n. Với n = 16, thời gian trung bình là: ___
[<A>]	4
Câu 31.
[AN]	stack<int> s;
s.push(10);
s.push(20);
s.push(30);
s.pop();
s.push(40);
cout << s.top();
Kết quả in ra màn hình là: ___
[<A>]	40
Câu 32.
[AN]	queue<int> q;
q.push(5);
q.push(10);
q.push(15);
q.pop();
q.push(20);
q.pop();
cout << q.front();
Kết quả in ra màn hình là: ___
[<A>]	15
Câu 33.
[AN]	stack<int> s;
int sum = 0;
for (int i = 1; i <= 4; ++i)
    s.push(i);
while (!s.empty()) {
    sum += s.top();
    s.pop();
}
cout << sum;
Tổng giá trị được in ra là: ___
[<A>]	10
Câu 34.
[AN]	queue<int> q;
for (int i = 1; i <= 6; i += 2)
    q.push(i);
q.pop();
q.pop();
cout << q.size();
Kết quả in ra là: ___
[<A>]	1
Câu 35.
[AN]	stack<int> s;
s.push(1);
s.push(2);
s.push(3);
s.pop();
s.pop();
s.push(4);
cout << s.size();
Kích thước cuối cùng của stack là: ___
[<A>]	2
Câu 36.
[AN]	queue<int> q;
q.push(1);
q.push(2);
q.push(3);
while (!q.empty()) {
    cout << q.front() << " ";
    q.pop();
}
Số lượng số được in ra là: ___
[<A>]	3
Câu 37.
[AN]	stack<int> s;
s.push(10);
s.push(20);
int a = s.top(); s.pop();
int b = s.top(); s.pop();
cout << a - b;
Giá trị in ra là: ___
[<A>]	10
Câu 38.
[AN]	queue<int> q;
for (int i = 1; i <= 4; ++i)
    q.push(i * 2);
q.pop();
cout << q.front();
Giá trị in ra là: ___
[<A>]	4
Câu 39.
[AN]	stack<int> s;
s.push(5);
s.push(10);
s.pop();
s.push(15);
cout << s.top() + s.size();
Giá trị in ra là: ___
[<A>]	17
Câu 40.
[AN]	queue<int> q;
q.push(2);
q.push(4);
q.push(6);
q.pop();
q.pop();
q.push(8);
cout << q.back();
Kết quả in ra là: ___
[<A>]	8
Câu 41. [R]	Khai báo cấu trúc nút cho ngăn xếp dùng danh sách liên kết đơn.
typedef struct Node {
    int data;
    struct Node *______ ;
} Node;
[<A>]	next
Câu 42.
[R]	Hàm kiểm tra ngăn xếp rỗng với con trỏ top.
int isEmpty(Node *top) {
    return ______ ;
}
[<A>]	top == NULL
Câu 43.
[R]	Hàm thêm phần tử vào hàng đợi sử dụng danh sách liên kết có con trỏ rear.
void enqueue(Node **rear, int value) {
    Node *newNode = (Node*)malloc(sizeof(Node));
    newNode->data = value;
    newNode->next = NULL;
    (*rear)->______ = newNode;
    *rear = newNode;
}
[<A>]	next
Câu 44.
[R]	Câu lệnh giải phóng phần tử đầu hàng đợi.
Node *temp = front;
front = front->next;
______ ;
[<A>]	free(temp)
Câu 45.
[R]	Khởi tạo ngăn xếp rỗng.
Node *top = ______ ;
[<A>]	NULL
Câu 46.
[R]	Câu lệnh khởi tạo một nút mới cho danh sách liên kết dùng trong ngăn xếp.
Node *newNode = (Node*) ______(sizeof(Node));
[<A>]	malloc
Câu 47.
[R]	Câu lệnh thêm phần tử vào đỉnh ngăn xếp (push).
newNode->next = top;
top = ______ ;
[<A>]	newNode
Câu 48.
[R]	Khai báo cấu trúc hàng đợi với hai con trỏ front và rear.
typedef struct Queue {
    Node *front;
    Node *______ ;
} Queue;
[<A>]	rear
Câu 49.
[R]	Hàm lấy phần tử khỏi ngăn xếp (pop).
int pop(Node **top) {
    if (*top == NULL) return -1;
    Node *temp = *top;
    int value = temp->data;
    *top = ______ ;
    free(temp);
    return value;
}
[<A>]	temp->next
Câu 50.
[R]	Hàm xem giá trị phần tử đầu hàng đợi.
int peek(Queue *q) {
    if (q->front == NULL) return -1;
    return q->front->______ ;
}
[<A>]	data
Câu 51.
[U]	Trong đoạn code dưới đây, dòng nào đảm bảo phần tử mới trở thành đỉnh mới của ngăn xếp?
Node *newNode = (Node*)malloc(sizeof(Node));
newNode->data = value;
newNode->next = top;
top = ______ ;
[<A>]	newNode
Câu 52.
[U]	Câu lệnh ______ đảm bảo liên kết phần tử mới vào cuối hàng đợi hiện tại.
Node *newNode = (Node*)malloc(sizeof(Node));
newNode->data = value;
newNode->next = NULL;
if (rear != NULL)
    rear->next = newNode;
rear = newNode;
[<A>]	rear->next = newNode
Câu 53.
[U]	Khi hàng đợi chỉ còn 1 phần tử, việc xóa phần tử đó yêu cầu cập nhật front và rear như thế nào?
if (front == rear) {
    free(front);
    front = rear = ______ ;
}
[<A>]	NULL
Câu 54.
[U]	Trong hàm kiểm tra rỗng cho hàng đợi, điều kiện nào phản ánh đúng trạng thái rỗng?
int isEmpty(Queue *q) {
    return q->front ______ NULL;
}
[<A>]	==
Câu 55.
[U]	Trong đoạn mã dưới đây, điền vào phần còn thiếu để đảm bảo vùng nhớ cũ được giải phóng sau khi cập nhật con trỏ top.
Node *temp = *top;
*top = temp->next;
______ ;
[<A>]	free(temp)
Câu 56.
[U]	Trong thao tác lấy phần tử ra khỏi hàng đợi, cập nhật con trỏ front để trỏ tới phần tử tiếp theo.
Node *temp = front;
front = ______ ;
free(temp);
[<A>]	front->next
Câu 57.
[U]	Trong ngăn xếp dùng mảng, điều kiện kiểm tra ngăn xếp đầy là:
int isFull() {
    return top ______ MAX_SIZE - 1;
}
[<A>]	==
Câu 58.
[U]	Khi thêm phần tử đầu tiên vào hàng đợi rỗng, cả front và rear đều phải được cập nhật:
if (front == NULL) {
    front = rear = ______ ;
}
[<A>]	newNode
Câu 59.
[U]	Trong ngăn xếp dùng mảng, để lấy giá trị phần tử trên đỉnh mà không xóa nó, ta dùng:
int peek() {
    return stack[______];
}
[<A>]	top
Câu 60.
[U]	Cập nhật rear sau khi thêm phần tử mới vào hàng đợi sử dụng danh sách liên kết:
rear->next = newNode;
______ = newNode;
[<A>]	rear
Câu 61.
[AP]	Hoàn thiện đoạn mã để đếm số phần tử trong ngăn xếp dùng danh sách liên kết.
int countStack(Node *top) {
    int count = 0;
    while (top != NULL) {
        count++;
        top = ______ ;
    }
    return count;
}
[<A>]	top->next
Câu 62.
[AP]	Viết đoạn mã kiểm tra xem phần tử x có tồn tại trong hàng đợi hay không.
int contains(Node *front, int x) {
    while (front != NULL) {
        if (front->data == x) return 1;
        front = ______ ;
    }
    return 0;
}
[<A>]	front->next
Câu 63.
[AP]	Viết đoạn mã để đảo ngăn xếp bằng cách sử dụng ngăn xếp phụ.
void reverseStack(Node **top) {
    Node *tempStack = NULL;
    while (*top != NULL) {
        int val = pop(top);
        push(&tempStack, val);
    }
    *top = ______ ;
}
[<A>]	tempStack
Câu 64.
[AP]	Viết đoạn mã tính tổng tất cả các phần tử trong hàng đợi.
int sumQueue(Node *front) {
    int sum = 0;
    while (front != NULL) {
        sum += front->data;
        front = ______ ;
    }
    return sum;
}
[<A>]	front->next
Câu 65.
[AP]	Viết đoạn mã để tìm phần tử lớn nhất trong ngăn xếp.
int findMax(Node *top) {
    int max = top->data;
    while (top != NULL) {
        if (top->data > max)
            max = top->data;
        top = ______ ;
    }
    return max;
}
[<A>]	top->next
Câu 66.
[AP]	Hoàn thiện đoạn mã để sao chép một hàng đợi sang hàng đợi khác.
void copyQueue(Node *src, Node **destRear) {
    while (src != NULL) {
        Node *newNode = (Node*)malloc(sizeof(Node));
        newNode->data = src->data;
        newNode->next = NULL;
        (*destRear)->next = newNode;
        *destRear = ______ ;
        src = src->next;
    }
}
[<A>]	newNode
Câu 67.
[AP]	Viết đoạn mã in tất cả phần tử trong ngăn xếp mà không thay đổi ngăn xếp.
void printStack(Node *top) {
    Node *current = top;
    while (current != NULL) {
        printf("%d ", current->data);
        current = ______ ;
    }
}
[<A>]	current->next
Câu 68.
[AP]	Đếm số phần tử chẵn trong hàng đợi.
int countEven(Node *front) {
    int count = 0;
    while (front != NULL) {
        if (front->data % 2 == 0)
            count++;
        front = ______ ;
    }
    return count;
}
[<A>]	front->next
Câu 69.
[AP]	Thêm phần tử vào ngăn xếp nhưng chỉ khi phần tử đó chưa tồn tại.
void pushUnique(Node **top, int x) {
    if (!contains(*top, x)) {
        Node *newNode = (Node*)malloc(sizeof(Node));
        newNode->data = x;
        newNode->next = *top;
        *top = ______ ;
    }
}
[<A>]	newNode
Câu 70.
[AP]	Viết đoạn mã giải phóng toàn bộ bộ nhớ của hàng đợi.
void clearQueue(Node **front) {
    Node *temp;
    while (*front != NULL) {
        temp = *front;
        *front = (*front)->next;
        ______ ;
    }
}
[<A>]	free(temp)
Câu 71.
[AN]	Phân tích đoạn mã và xác định phần bị thiếu gây lỗi rò rỉ bộ nhớ khi xóa phần tử trong hàng đợi:
Node *temp = front;
if (front == rear) {
    front = rear = NULL;
} else {
    front = front->next;
}
______ ;
[<A>]	free(temp)
Câu 72.
[AN]	Đoạn mã dưới đây có nguy cơ lỗi truy cập bộ nhớ nếu thiếu dòng lệnh nào?
Node *newNode = (Node*)malloc(sizeof(Node));
if (______ ) return; // kiểm tra cấp phát bộ nhớ thất bại
newNode->data = value;
newNode->next = top;
top = newNode;
[<A>]	newNode == NULL
Câu 73.
[AN]	Phân tích đoạn mã thêm phần tử vào hàng đợi. Nếu thiếu dòng sau thì hàng đợi hoạt động sai khi chỉ có 1 phần tử:
if (front == NULL) {
    front = rear = newNode;
    return;
}
rear->next = newNode;
rear = ______ ;
[<A>]	newNode
Câu 74.
[AN]	Trong hàm pop của ngăn xếp, nếu không cập nhật *top, phần tử vừa xóa sẽ vẫn là đỉnh, dẫn đến lỗi logic vòng lặp vô hạn hoặc pop sai. Hãy điền câu lệnh còn bị thiếu.
int pop(Node **top) {
    if (*top == NULL) return -1;
    Node *temp = *top;
    int val = temp->data;
    ______ ;   // thiếu dòng này sẽ giữ nguyên top
    free(temp);
    return val;
}
[<A>]	*top = temp->next
Câu 75.
[AN]	Phân tích đoạn mã xử lý xóa phần tử trong hàng đợi. Nếu không kiểm tra front == NULL, đoạn mã sau sẽ gây lỗi truy cập con trỏ NULL. Hãy điền điều kiện còn thiếu:
int dequeue() {
    if (______ ) return -1;
    Node *temp = front;
    int value = temp->data;
    front = front->next;
    free(temp);
    return value;
}
[<A>]	front == NULL
